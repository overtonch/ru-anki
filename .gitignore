# deps
.venv/
__pycache__/
*.pyc

# local state — the DB is the user's data, backed up separately to iCloud
vocab.db
vocab.db-wal
vocab.db-shm
vocab.db.bak-*

# generated / exported
*.apkg
out/

# early-development scratch (superseded by app/)
transcript.txt
cand_v1.json
