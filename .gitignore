# deps
.venv/
__pycache__/
*.pyc
.pytest_cache/

# local state — the DB is the user's data, backed up separately to iCloud
vocab.db
vocab.db-wal
vocab.db-shm
vocab.db.bak-*

# generated / exported
*.apkg
out/

# stress dictionary — built from the committed app/data/stress/*.json.gz on first run
app/data/stress.db
app/data/stress.db-*

# early-development scratch (superseded by app/)
transcript.txt
cand_v1.json
